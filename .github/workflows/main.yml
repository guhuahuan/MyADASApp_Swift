name: FSD V4.0 PRODUCTION BUILD

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: macos-14

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          lfs: true # 必须开启 LFS 否则模型文件只是指针

      - name: Create Info.plist
        run: |
          cat <<EOF > Info.plist
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>CFBundleExecutable</key><string>MyADASApp_Swift</string>
              <key>CFBundleIdentifier</key><string>com.fsd.v4.master</string>
              <key>CFBundleName</key><string>FSD MASTER</string>
              <key>CFBundlePackageType</key><string>APPL</string>
              <key>CFBundleShortVersionString</key><string>4.0</string>
              <key>CFBundleVersion</key><string>1</string>
              <key>LSRequiresIPhoneOS</key><true/>
              <key>MinimumOSVersion</key><string>17.0</string>
              <key>NSCameraUsageDescription</key><string>AI道路感知与避障</string>
              <key>NSLocationWhenInUseUsageDescription</key><string>计算时速与航向</string>
              <key>NSMotionUsageDescription</key><string>3D轨迹动态修正</string>
              <key>UISupportedInterfaceOrientations</key>
              <array><string>UIInterfaceOrientationLandscapeRight</string></array>
              <key>UILaunchScreen</key><dict/>
          </dict>
          </plist>
          EOF

      - name: Swift Build
        run: |
          SDK_PATH=$(xcrun --sdk iphoneos --show-sdk-path)
          swift build -c release \
            --triple arm64-apple-ios17.0 \
            -Xswiftc -sdk -Xswiftc "$SDK_PATH" \
            -Xcc -isysroot -Xcc "$SDK_PATH" \
            -Xlinker -sectcreate -Xlinker __TEXT -Xlinker __info_plist -Xlinker Info.plist

      - name: Assemble & Verify IPA
        run: |
          rm -rf Payload
          mkdir -p Payload/MyADASApp_Swift.app
          
          # 1. 拷贝二进制
          BIN=$(find .build -type f -name "MyADASApp_Swift" | grep "release" | head -n 1)
          cp "$BIN" Payload/MyADASApp_Swift.app/MyADASApp_Swift
          chmod +x Payload/MyADASApp_Swift.app/MyADASApp_Swift
          
          # 2. 拷贝模型 (强制校验)
          MODEL=$(find . -name "*.mlmodelc" -type d | head -n 1)
          if [ -z "$MODEL" ]; then echo "❌ Error: Model NOT found!"; exit 1; fi
          cp -r "$MODEL" Payload/MyADASApp_Swift.app/
          
          # 3. 拷贝权限文件
          cp Info.plist Payload/MyADASApp_Swift.app/
          
          # 4. 打包并检查体积
          zip -r FSD_MASTER.ipa Payload
          ls -lh FSD_MASTER.ipa
